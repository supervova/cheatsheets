```javascript
// Число
const age = 18;

// Строка
const name = 'Jane';

// Логический (булевый) тип
let truth = false;

let a;

// undefined
truth = typeof a;

// null
a = null;

// Массив
const sheets = ['HTML', 'CSS', 'JS'];

// Объект
const fullName = { first: 'Jane', last: 'Doe' };
```
